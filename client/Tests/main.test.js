import { it, expect, describe } from "vitest";

describe("main", () => {
  it("should result to pass", () => {
    expect(1).toBeTruthy(); //assertion that 1 is a   using the tobeTruthy mather
  });
});
